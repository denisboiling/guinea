<%= semantic_form_for [:admin, resource] do |f| %>
  <%= f.inputs do %>
    <%= f.input :url %>
    <%= f.input :logo %>

    <%= image_tag resource.logo.thumb.url %>
  <% end %>

  <%= f.actions %>
<% end %>
